<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Disk Configuration
    |--------------------------------------------------------------------------
    |
    | هنا تحدد الـ disk المستخدم لتخزين النسخ الاحتياطية.
    | يمكنك استخدام أي disk معرف في config/filesystems.php
    | مثل: local, public, s3, spaces, وغيرها.
    |
    */
    'disk' => env('DUYUN_BACKUP_DISK', 'public'),

    /*
    |--------------------------------------------------------------------------
    | Backup Path
    |--------------------------------------------------------------------------
    |
    | هنا تحدد المسار داخل الـ disk الذي سيتم تخزين النسخ فيه.
    | يمكنك تغييره مثلاً إلى "exports" أو "archives/backups"
    |
    */
    'path' => env('DUYUN_BACKUP_PATH', 'backups'),

    /*
    |--------------------------------------------------------------------------
    | Temporary Files Path
    |--------------------------------------------------------------------------
    |
    | هذا المسار يستخدم لحفظ الملفات المؤقتة أثناء عملية التصدير.
    | تأكد أن هذا المسار قابل للكتابة.
    |
    */
    'temp_path' => storage_path('app/tmp'),

    /*
    |--------------------------------------------------------------------------
    | Default File Format
    |--------------------------------------------------------------------------
    |
    | يمكنك تحديد صيغة التصدير الافتراضية مثل: excel, csv, json
    | لاحقاً يمكنك إضافة Exporters متعددة وتبديلها هنا.
    |
    */
    'default_exporter' => 'excel',

    /*
    |--------------------------------------------------------------------------
    | Auto Delete After Backup
    |--------------------------------------------------------------------------
    |
    | عند تفعيل هذا الخيار، سيتم حذف البيانات من قاعدة البيانات
    | بعد نجاح عملية النسخ الاحتياطي.
    |
    */
    'auto_delete' => env('DUYUN_BACKUP_AUTO_DELETE', false),

    /*
    |--------------------------------------------------------------------------
    | File Name Prefix
    |--------------------------------------------------------------------------
    |
    | يمكنك تحديد بادئة لأسماء ملفات النسخ الاحتياطية.
    | مثال: 'backup_' أو 'export_'
    |
    */
    'file_prefix' => env('DUYUN_BACKUP_PREFIX', 'backup_'),
];
